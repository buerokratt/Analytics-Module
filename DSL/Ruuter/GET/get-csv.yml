get_message:
  call: http.post
  args:
    url: http://resql:8082/test-csv-response
  result: message

return_value:
  headers:
    Content-disposition: "attachment;filename=metrics.csv"
  return: ${message.response.body[0].response}
