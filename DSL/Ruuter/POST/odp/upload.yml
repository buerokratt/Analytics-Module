get_metrics:
  template: odp/metrics
  requestType: post
  body:
    metrics: ${incoming.body.metrics}
    start: ${incoming.body.start}
    end: ${incoming.body.end}
    period: ${incoming.body.updateIntervalUnit}
  result: metricDataCSV

#upload:
#  call: http.post
#  args:
#    url: https://avaandmed.eesti.ee/api/organizations/my-organizations/${orgId}/datasets/${datasetId}/upload
#    file:
#      name: "metrics.csv"
#        content: ${metricDataCSV}
#  result: result

#return:
#  return: ${result.response.body}
